import { createElement, ReactElement } from "react";
import Page from "../components/Page";

export default function Home(): React.ReactElement {
  // Retourne un élément React (OK sans JSX)
  return createElement(Page, null);
}









